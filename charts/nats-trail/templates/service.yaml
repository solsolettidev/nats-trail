apiVersion: v1
kind: Service
metadata:
  name: {{ include "nats-trail.fullname" . }}
  labels:
    {{- include "nats-trail.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "nats-trail.selectorLabels" . | nindent 4 }}
